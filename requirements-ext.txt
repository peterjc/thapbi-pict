# These are external dependencies (not available via PyPI and pip),
# best installed with:
#
# $ conda install --file requirements-ext.txt
#
# We assume you have the conda-forge and bioconda channels.

biom-format >=2.1.15
blast
flash
graphviz
sqlite >=3
unzip
vsearch >=2.23.0
